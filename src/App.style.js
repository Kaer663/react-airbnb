import styled from "styled-components";

export const AppWrapper = styled.div`
  .app{
    .app-header{
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      z-index:1;
      background-color: #fff;
    }
  }
`;