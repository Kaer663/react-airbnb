import styled from "styled-components";

export const HeaderWrapper = styled.div`
  section {
    color: #222;
    font-size: 22px;
    line-height: 26px;
    font-weight: 600;
    .title {
      font-size: 1.4rem;
    }
  }
  .subtitle {
      font-size: 16px;
      line-height: 24px;
      margin-top: 12px;
    }
`